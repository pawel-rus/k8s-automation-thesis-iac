serviceAccount:
  create: false      
  name: admin-{{ project_env }}

rbac:
  clusterAdminRole: true

ingress:
  enabled: false 

kong:
  proxy:
    http:
      enabled: true

ingress:
  enabled: false